import React from 'react'

type Props = {
  workspaceId: string;
}

const WorkspaceSearch = (props: Props) => {
  const { workspaceId } = props;
  return (
    <div>WorkspaceSearch</div>
  )
}


export default WorkspaceSearch;